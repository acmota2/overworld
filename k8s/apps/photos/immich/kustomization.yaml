---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

secretGenerator:
  - name: immich-psql-secret
    namespace: photos
    options:
      disableNameSuffixHash: true
    envs:
      - db.env

resources:
  - app.yaml
  - configmap.yaml
  - psql.yaml
  - redis.yaml
  - storage.yaml
