---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dragonfly
  namespace: flux-system
spec:
  values:
    resources:
      limits:
        cpu: "200m"
        memory: "256Mi"
      requests:
        cpu: "300m"
        memory: "128Mi"
