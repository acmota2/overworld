---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dragonfly
  namespace: flux-system
spec:
  values:
    storage:
      storageClassName: longhorn
      resources:
        requests:
          storage: 10Gi
