---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: psql-cluster
  namespace: db
spec:
  instances: 2
  storage:
    size: 2Gi
    storageClass: longhorn
  managed:
    roles:
      - name: immich
        ensure: present
        login: true
        passwordSecret:
          name: immich-db-creds
      - name: docmost
        ensure: present
        login: true
        passwordSecret:
          name: docmost-db-creds
      - name: vikunja
        ensure: present
        login: true
        passwordSecret:
          name: vikunja-db-creds
  bootstrap:
    initdb:
      database: appdb
      owner: appuser
      encoding: UTF8
  monitoring:
    enablePodMonitor: true
  resources:
    requests:
      cpu: 200m
      memory: "1Gi"
    limits:
      cpu: 1000m
      memory: "1.5Gi"
