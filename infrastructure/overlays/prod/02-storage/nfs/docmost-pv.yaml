---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: docmost-nfs-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  mountOptions:
    - hard
    - nfsvers=4.2
  nfs:
    path: /docmost
    server: ${NFS_SERVER_ADDRESS}
